{% extends 'base.twig' %}
{% block body%}

<h2>Статьи</h2>

{{include ('pagination.twig')}}

{% for item in data %}
    <div class="note">
        <p>
         <h3>{{ item.header }}</h3>
         <div style="text-align: right;">
         <a href="/articles/{{ item.url }}">Читать</a>
         </div>
        </p>
    </div>
{% endfor %}

{{include ('pagination.twig')}}

{% endblock %}