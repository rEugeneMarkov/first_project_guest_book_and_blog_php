{% extends 'base.twig' %}
{% block body%}

<h2>Статьz</h2>

{% for item in data %}
<div class="note">
    <p>
        <span class="date">{{ item.date }}</span>
        <span class="name">{{ item.name }}</span>
    </p>
    <p>
        {{ item.content }}
    </p>
</div>

{% endfor %}
{% endblock %}